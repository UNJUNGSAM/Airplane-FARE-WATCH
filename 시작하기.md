# 🚀 처음 시작하기 — 따라만 하세요 가이드

> 프로그램은 이미 다 만들어져 있습니다. 이제 **열쇠 3개를 발급받아 넣고, 올리고, 켜기**만 하면 됩니다.

## 용어 아주 쉬운 설명

| 용어 | 뜻 |
|---|---|
| 저장소(Repository) | GitHub라는 사이트의 **코드 보관함**. 구글 드라이브 폴더라고 생각하세요 |
| 푸시(Push) | 내 컴퓨터의 파일을 그 보관함으로 **올리는 동작** |
| 시크릿(Secrets) | 열쇠(API 키)를 남들이 못 보게 **안전하게 보관하는 곳** |
| API 키 | 외부 서비스(Gemini 등)를 쓸 수 있게 해주는 **열쇠 문자열** |

---

## 1단계. 열쇠 3개 만들기 (약 10분)

### 🔑 열쇠 1: Gemini API 키
1. 브라우저에서 **https://aistudio.google.com/apikey** 접속 (구글 계정 로그인)
2. **「API 키 만들기」** 버튼 클릭
3. 나오는 문자열(`AIza` 로 시작)을 복사해서 메모장에 붙여넣어 두세요

### 🔑 열쇠 2: 텔레그램 봇 토큰 + chat ID
1. 텔레그램 앱에서 **@BotFather** 검색 → 대화 시작
2. `/newbot` 입력 → 봇 이름을 정하면 **토큰**을 줍니다 (`123456789:ABCdef...` 형태) → 복사
3. **방금 만든 봇을 검색해서 대화창을 열고 "안녕" 이라고 한 번 보내세요** (중요! 안 하면 알림이 안 옵니다)
4. 브라우저에서 `https://api.telegram.org/bot<아까복사한토큰>/getUpdates` 접속
5. 화면에서 `"chat":{"id": 123456789` 를 찾아 **그 숫자**가 chat ID 입니다 → 복사

### 🔑 열쇠 3: GitHub 토큰 (클라우드 배포할 때만 필요)
1. **https://github.com** 로그인 → 오른쪽 위 내 아바타 → **Settings**
2. 왼쪽 맨 아래 **Developer settings**
3. **Personal access tokens → Tokens (classic)** → **Generate new token (classic)**
4. Note: 아무 이름 / Expiration: 90일 이상 / 체크박스에서 **repo** 에 체크
5. **Generate token** → `ghp_` 로 시작하는 문자열 복사 (**페이지를 벗어나면 다시 못 봅니다!**)

---

## 2단계. 내 컴퓨터에서 열쇠 넣고 테스트 (약 5분)

✅ 이미 `.env` 파일은 만들어져 있습니다.

1. VSCode에서 프로젝트 폴더의 **`.env`** 파일을 열세요
2. `your_gemini_api_key_here` 자리에 Gemini 키, 토큰/ID 자리에 텔레그램 값 붙여넣고 **저장**
   - `GITHUB_TOKEN`, `GITHUB_REPO` 는 지금은 비워둬도 됩니다 (로컬 테스트에는 불필요)
3. VSCode 터미널(Ctrl+`) 에 입력:
   ```
   streamlit run streamlit_app/app.py
   ```
4. 브라우저가 열리면 상단 헤더 탭의 **조건 등록** 페이지에서 자유롭게 적어보세요:
   - `다음 달 말 도쿄 3박 4일, 45만원 이하면 알려줘`
   - `9월 23일 오후 8시 이후 or 9월 24일 새벽·오전 출발, 다낭·나트랑·푸꾸옥 직항 중 가격 내려가면 알려줘`
     → **목적지·날짜·시간대 조합별로 여러 조건이 한 번에 등록됩니다!**
5. **항공편 조회** 페이지에서 **지금 조회** 를 누르면 항공사 로고와 함께
   항공편 목록(출발·도착 시각)이 표시되고, **구글 항공권 확인** 링크로 바로 이동할 수 있습니다.
6. **대시보드**에서 국가별로 묶인 표의 판정 배지(역대 최저가 등)와 증감률을 확인하세요.

---

## 3단계. GitHub에 올리기 (= "저장소 푸시")

Streamlit Cloud에 앱 올려본 적 있으니 익숙할 거예요. **VSCode로 3번만 클릭**하면 됩니다:

1. VSCode 왼쪽 줄에서 세 번째 아이콘(**분기 모양, 소스 제어**) 클릭
2. **「Publish to GitHub」** 버튼 클릭 → GitHub 로그인
3. 저장소 이름 확인하고 **공개(public)** 로 게시 (공개여야 Actions가 무료입니다)

이걸로 끝! 이제 내 컴퓨터 파일이 GitHub 보관함에 올라갔습니다.

---

## 4단계. 자동 감시 켜기 (GitHub Secrets 등록)

1. 브라우저에서 내 GitHub 저장소 페이지 열기
2. **Settings → Secrets and variables → Actions** → **New repository secret** 버튼으로 아래 3개 등록:
   | Name | Value |
   |---|---|
   | `GEMINI_API_KEY` | AIza... |
   | `TELEGRAM_BOT_TOKEN` | 123456:ABC... |
   | `TELEGRAM_CHAT_ID` | 123456789 |
3. 저장소의 **Actions** 탭 클릭 → *flight-monitor* 선택 → **Enable workflows** 클릭
4. **Run workflow** 버튼을 눌러 1회 테스트 실행 → 초록불 ✅ 이면 성공!

이제 **30분마다 컴퓨터를 꺼둬도 구글 서버가 대신 가격을 확인**하고, 핫딜이 뜨면 텔레그램으로 알려줍니다.

---

## 5단계. Streamlit Cloud 배포 (어디서나 대시보드 보기)

전에 앱 올리던 것과 똑같습니다:

1. **https://share.streamlit.io** → **New app**
2. 방금 올린 저장소 선택, Branch: `main`
3. **Main file path: `streamlit_app/app.py`** ← 꼭 이렇게!
4. Deploy
5. 앱 화면 오른쪽 아래 **Manage app** → ⋮ 메뉴 → **Settings** → **Secrets** 에 아래를 붙여넣고 값만 교체:

```toml
GEMINI_API_KEY = "AIza..."
TELEGRAM_BOT_TOKEN = "123456:ABC..."
TELEGRAM_CHAT_ID = "123456789"
APP_PASSWORD = "원하는_비밀번호"
GITHUB_TOKEN = "github_pat_..."
GITHUB_REPO = "내아이디/저장소이름"
GITHUB_BRANCH = "main"
```

> ⚠️ `APP_PASSWORD`는 **반드시** 넣으세요. 없으면 주소를 아는 누구나 감시 조건을
> 추가·삭제하고 텔레그램 발송까지 시킬 수 있습니다.
>
> 💡 `GITHUB_TOKEN`까지 넣어야, 대시보드에서 조건을 바꿨을 때
> 자동 감시에도 바로 반영됩니다.

---

## ❓ 잘 안 될 때

### 🔸 getUpdates 접속했더니 {"ok":true,"result":[]} 만 나오는 경우

`result:[]` 는 **"봇이 아직 받은 메시지가 없다"** 뜻입니다. 토큰은 정상이에요!
아래 순서대로 하면 해결됩니다:

1. 텔레그램에서 **내가 만든 봇의 이름**을 검색하세요
   - ⚠️ 주의: `@BotFather`가 아니라, `/newbot`으로 만들 때 **내가 지은 봇 이름**(끝에 bot 붙는 것)입니다
2. 봇 대화창을 열고 아래쪽 파란 **「시작(START)」** 버튼을 누르거나, "안녕" 이라고 입력해 보내세요
3. **바로** 브라우저의 getUpdates 페이지를 새로고침(F5) 하세요
4. 이제 `"chat":{"id": 123456789, ...}` 가 나타납니다 → 그 숫자가 chat ID!

> 💡 그래도 안 되면: 텔레그램에서 `@userinfobot` 을 찾아 아무 메시지를 보내면
> 내 ID를 알려줍니다. 그 숫자를 chat ID로 쓰세요 (단, 내 봇 대화창에서 START를 누르는 건 필수!)

### 기타 문제

| 증상 | 해결 |
|---|---|
| 텔레그램 알림이 안 옴 | 봇에게 먼저 "안녕"이라고 보냈는지 확인 (2단계-열쇠2의 3번) |
| Gemini 503 UNAVAILABLE 오류 | 구글 서버가 일시적으로 혼잡한 것입니다. 프로그램이 자동으로 재시도·대체 모델 전환을 하므로 **30초쯤 기다렸다가 다시 눌러보세요** |
| Gemini 404 / model not found | `.env`의 `GEMINI_MODEL` 값에 오타가 있는지 확인 (기본값 `gemini-3.6-flash`) |
| 지금 확인 눌렀는데 실패 | 잠시 후 다시 시도 (구글이 일시적으로 막을 수 있음, 프로그램이 3번 재시도합니다) |
| Actions가 회색으로 멈춰있음 | Actions 탭에서 Enable workflows 했는지 확인 |
| 대시보드에서 바꾼 조건이 자동감시에 반영 안 됨 | Streamlit Secrets에 GITHUB_TOKEN/GITHUB_REPO 제대로 넣었는지 확인 |

더 자세한 기술 문서는 [README.md](README.md) 를 참고하세요.
